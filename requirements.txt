# ── Core data (serialize_t4.py, prepare_eval_data*.py, score_eval.py) ─────────
pandas>=2.0
pyarrow>=14.0
tqdm>=4.65
tabulate>=0.9       # required by pandas .to_markdown() used in serialize_t4.py

# ── ML: tokenizer + generation (prepare_eval_data*.py, serialize_t4.py) ───────
# torch: bfloat16 + device_map="auto" needs >= 2.1
# transformers: LLaMA-3.1 and Apertus tokenizer support added in 4.43
# NOT needed for --smoke-test
torch>=2.1
transformers>=4.43
